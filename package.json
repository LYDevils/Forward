{
  "name": "forward-widgets",
  "version": "1.0.0",
  "description": "Forward widget source pack",
  "main": "ForwardWidgetSource.js",
  "scripts": {
    "test": "node forwardSourceTest.js",
    "build:widgets": "node build-widget-artifacts.js",
    "build:fwd": "node generate-widget-index.js",
    "build": "npm run build:widgets && npm run build:fwd"
  }
}
